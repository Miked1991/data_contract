# requirements.txt
# Data Contract Enforcer - Essential Dependencies Only
# Tested on Windows + Python 3.11/3.12

# ============================================
# CORE DATA PROCESSING (Required)
# ============================================
pandas==2.1.4              # DataFrame operations for profiling
numpy==1.26.3              # Numerical operations

# ============================================
# FILE FORMATS (Required)
# ============================================
pyyaml==6.0.1              # YAML contract files
jsonschema==4.19.2         # JSON Schema validation

# ============================================
# GRAPH OPERATIONS (Required for lineage)
# ============================================
networkx==3.1              # Lineage graph traversal

# ============================================
# GIT OPERATIONS (Required for blame attribution)
# ============================================
gitpython==3.1.41          # Git blame and history

# ============================================
# CLI & UTILITIES (Required)
# ============================================
click==8.1.7               # Command-line interface
python-dotenv==1.0.0       # Environment variables

# ============================================
# DATA PROFILING (Optional but recommended)
# ============================================
ydata-profiling  
setuptools
# Use pandas built-in profiling instead (no extra packages)

# ============================================
# DATA QUALITY (Optional)
# ============================================
# great-expectations removed (too heavy, optional)

# ============================================
# AI EXTENSIONS (Optional - only if using LangSmith)
# ============================================
# langchain==0.1.9         # Only if using LangChain
# langsmith==0.1.0         # Only if using LangSmith traces
# openai==1.12.0           # Only if using OpenAI embeddings

# ============================================
# REPORT GENERATION (Optional - for PDF reports)
# ============================================
# weasyprint==60.2         # Only if generating PDF reports
# jinja2==3.1.3            # Only if using HTML templates